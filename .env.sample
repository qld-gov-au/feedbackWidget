# reCAPTCHA v3 site keys (public — safe to commit to .env.sample, NOT the secret keys)
# Each key must be paired with the matching secret key configured in the SSQ backend
RECAPTCHA_DEV=
RECAPTCHA_PROD=
